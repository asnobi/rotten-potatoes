%h1 Login

= form_tag login_create_path do

  = label :user, :id, 'User-ID'
  = text_field :user, 'user_id', :id => 'login_user'

  = label :user, :email, 'E-mail'
  = text_field :user, 'email', :id => 'login_email'
  


  = submit_tag 'Login To My Account' , :id => 'login_create'

= link_to 'Sign-up for an account', new_user_path
